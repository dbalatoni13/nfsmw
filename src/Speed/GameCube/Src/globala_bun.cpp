// globala.bun embebido: asset binario global (fallback cuando no está en disco).
// Contenido extraído del .o original (símbolo bin_globala_bun, 89716 bytes).

extern "C" unsigned char bin_globala_bun[89716] = {
#include "Speed/GameCube/Src/globala_bun.inc"
};
