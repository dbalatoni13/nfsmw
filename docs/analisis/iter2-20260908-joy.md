# Iteracion 2, 2026-09-08: JoyE, productores reales del par ausente

Base HEAD `8853122e`, respetando todas las modificaciones locales ajenas.
Leidos brief-r46, r46-game, comentarios de JoyE y r40-platform. No se repiten
recortes C, consumidores ficticios del resultado muerto, pines de los recortes
ni banderas de planificacion. **Ganancia 0 B; produccion intacta.**

## Hipotesis acotada

`ActualReadJoystickData__Fv`: objetivo 1588 B, fuente 1580 B, 99,24433 %, 19
filas distintas. Las ausencias son `mr r9,r0` y `andi. r11,r9,0x8000`, alrededor
del `stb` de stickX. Se prueba emitir esos productores reales mediante ASM
inline con entradas definidas, sin inventar un consumidor C del resultado.
El resultado de `andi.` no se usa; se declara volatile para conservar esa
instruccion muerta del objetivo y se declara su clobber `cr0`.

Todos los ensayos son privados. La SourceList envolvente conserva todos los
includes y flags reales; solo cambia el include de JoyE. El control produce
secciones runtime **y sus secciones de relocacion crudas identicas** al objeto
real, no solo el mismo porcentaje.

| variante | B fuente | similitud | filas distintas |
|---|---:|---:|---:|
| control | 1580 | 99,24433 % | 19 |
| `copy_mask`: copia a local, store C, andi productor separado | 1588 | 98,27456 % | 40 |
| `mask_only`: solo andi productor sobre v tras el store | 1584 | 99,54660 % | 13 |
| `copy_mask_tied`: mr/store/andi juntos, salida early-clobber y operando memoria | 1588 | 99,65995 % | 24 |
| `mask_copy_input`: copia early-clobber separada, andi devuelve v | 1592 | 98,01008 % | 41 |

Tras las primeras tres medidas se reevaluo una sola hipotesis derivada:
`mask_only` arregla el intercambio global RealTimer/0x4330 (r18/r19), pero
introducir la copia independiente vuelve a romperlo y obliga a un mr adicional.
Es la cuarta medida, no un barrido ciego de registros.

## Resultado causal y limites

- Es posible producir las dos instrucciones y recuperar el tamano con ASM
  limitado; no implica recuperar el reparto original. La afirmacion de
  r46-game de que las otras 17 filas caerian solas no se confirma por esta via.
- La secuencia atomica de tres instrucciones conserva el orden del store y
  tiene tamano exacto, pero deja 24 filas: tambien intercambia r14/r15. Su
  porcentaje mayor no es una mejora del numero de instrucciones exactas.
- `mask_only` corrige las seis filas de r18/r19 sin tocar la rama de volante,
  pero conserva los dos recortes en r8 en lugar de r10/r11 y los tres operandos
  conmutados en la rama Logitech. Falta aun el mr: no hay cierre parcial.
- Las cinco copias posteriores siguen siendo artefactos UNSPEC del backend;
  no se les invento un consumidor ni una variable sin inicializar.
- No se conserva ASM en la fuente real. Tampoco se reabren los pines de r40,
  que eliminaban copias necesarias de los recortes.

## Comprobacion

`scratchpad/codex_20260908_iter2_joy/audit.py`: PASS. En cada variante solo
cambia la metrica de ActualReadJoystickData; inventario comparable conservado.
Fuente y objeto reales permanecen byte-identicos a la entrada:

- JoyE.cpp SHA-256 `7a08bf0f26f8f6ffccad7fa7a4d2aa6c3588d6926b007ad40900f072cf7f405c`.
- zPlatform.o SHA-256 `24295cfdb801dae742c2b12b441040347e169a48b137c43d37a9f71973b8abc3`.

Los JSON completos, fuentes y objetos privados estan en ese directorio.
No hubo compilacion ni escritura sobre produccion; no se tocaron cabeceras,
configuracion, splits, commits, ni fuentes/objetos de los otros agentes.
