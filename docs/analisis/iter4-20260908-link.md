# Iter4: promociones autorizadas FSasync y vmbase, +5.284 B linked

El usuario autorizo durante esta tanda: **«haz las promociones a linked cuando
quieras»**. Esto levanta la espera para promociones respaldadas por el DOL;
no autoriza retener regresiones ni cambios ajenos a ese trabajo.

Se integraron dos promociones separadas, validando el DOL tras cada una. No se
modifico ningun split, simbolo, keep, linker script, cabecera compartida o dato.

## FSasync

Se cambia exclusivamente la entrada activa:

```python
Object(Matching, "FSasync.c", source="LibSN/FSasync.c", cflags=[*cflags_libc, "-G0"])
```

La entrada inactiva `LibSN/FSasync.c` sigue NonMatching. La fuente y el objeto
actuales no se recompilaron ni modificaron: son el cierre y reorden de iter2,
13/13 funciones,2104 B, con runtime y raw relocs identicos a su DOL privado.

`python configure.py` regenera solo manifiestos, sin ninja. La lista de616
objetos cambia exactamente una ruta de obj/FSasync.o a src/FSasync.o.
El enlace real da DOL SHA original. Linked pasa757604->759708 B;484 unidades
completas, datos y todos los porcentajes de funciones intactos.

## vmbase

Se integran los siete externs y macros de alias probados en iter3. Los nombres
de datos hacen referencia a lo que ya exporta auto_09_804FFED8_sbss.o:

| nombre fuente | alias existente |
|---|---|
| g_vmBasePageTable | g_vmBasePageTable_804FFED8 |
| g_vmBaseVMReversePageTable | g_vmBaseVMReversePageTable_804FFEDC |
| g_vmBaseLockedPageTable | g_vmBaseLockedPageTable_804FFEE0 |
| cbVMSwapPageIn | cbVMSwapPageIn_804FFEE4 |
| g_baseInitialized | g_baseInitialized_804FFEE8 |
| g_originalSR7 | g_originalSR7_804FFEEC |
| g_originalSDR1 | g_originalSDR1_804FFEF0 |

El bloque declarativo y una nota son el unico cambio de fuente respecto al
cierre de380 B; fuera de ese bloque la fuente permanece byte-identica. No se
definen los28 B de .sbss en esta TU ni se eliminan del objeto automatico. Su
contenido, posiciones y objeto original se conservan: la fuente promocionada
es de codigo, no una reconstruccion nueva de la propiedad fisica de esos datos.

Compilacion exacta de un objeto: 1 OK. Las30 funciones vuelven a pasar el
auditor ELF, sin overrides. Runtime bytes/raw relocs iguales al objeto privado
probado. Se cambia la unica entrada `Object(Matching, "LibSN/vmbase.c")`.
La segunda regeneracion conserva los616 objetos y sustituye solo la segunda
ruta. El DOL combinado sigue identico al original.

Linked pasa759708->762888 B, **19,332964 %**;485/616 unidades completas.
No se cuentan otros3180 B matched: los30 cuerpos ya estaban exactos en iter3.

## Gate vigente

```text
python scratchpad/codex_20260908_iter4_link/audit_final.py
python scripts/audit.py FSasync
python scripts/audit.py LibSN/vmbase
```

El gate compara el cambio exacto de las dos entradas de configure, las530
especificaciones de compilacion y reglas, el orden de los616 objetos antes,
en el punto FSasync y al final, y los21 archivos protegidos de configuracion
y cambios locales del usuario. No basta con un contador de unidades.

Tambien compara los18432 registros completos de funciones del informe: todos
identicos. El censo directo y las33 SourceLists/15493 entradas no cambian.
Datos exactos443781 B y datos linked355348 B se mantienen. Los DOL guardados
de ambas integraciones y el DOL real tienen4541888 B y SHA-1:

**9619ba57c9919f95f7f2ac951a2166a3517f91e3**

Baselines y pruebas historicas permanecen inmutables. Los gates de iter2/iter3
que exigian configure/build.ninja sin cambios o vmbase sin aliases **ahora son
historicos**: no sobrescribir sus hashes para hacerlos pasar. El gate de esta
tanda comprueba las mutaciones autorizadas y conserva sus copias anteriores.

La autoridad recibida permite continuar otras promociones verificadas. No
confundirla con permiso para retirar P6 de ENVIRO_AEMS, una cabecera compartida
cuya regresion previa y posible recuperacion se documentan por separado.
