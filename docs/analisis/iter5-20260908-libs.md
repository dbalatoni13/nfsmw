# Iter5 — `pathnode::PATHI_calcwaitbeat`

**Resultado: negativo, 0 B retenidos.** Se eligio `PATHI_calcwaitbeat` frente
a `sfir`: la primera conserva 336/336 B y solo 14 diferencias FPR, mientras
que `sfir` sigue bloqueada por la familia PRE/CSE1 ya medida en r43/r46.

La base real queda en 98,86905 %. El DWARF nombra `scalar` en f11; el objetivo
emite `fdivs f11,f11,f0`, pero el C actual solo fija el numerador auxiliar
`notes` a f11 y GCC crea el resultado `scalar` en f8. Las formas C, pines y
barreras que atacaban este reparto estaban agotadas, pero no se habia probado
el productor aritmetico real localizado.

## Tres pruebas

| forma | cambio causal | resultado contra objetivo |
|---|---|---:|
| `c1` | `fdivs` real, salida `=f`, numerador y divisor como inputs C separados | 340 B / 63,02381 % |
| `c2` | `fdivs` real read/write sobre `notes`, seguido de `scalar = notes` | 340 B / 63,02381 % |
| `c3` | materializa `fevery` y `foffset` antes del `fdivs` de `c1` | 340 B / 59,083332 % |

`c1` demuestra que la restriccion es suficiente para obtener exactamente el
productor buscado (`fdivs f11,f11,f0`). Sin embargo, el bloque opaco impide que
las conversiones posteriores de `every` y `offset` se adelanten como en el
objetivo. Esos enteros cruzan el bloque y GCC pasa de guardar solo r31 a guardar
r28--r31; la funcion crece 4 B y el reparto global se degrada. `c2` cambia solo
la asignacion de los registros que materializan `$LC1`. `c3` intenta quitar esa
vida cruzada de forma semantica, calculando primero los dos valores reales, pero
el scheduler genera otro reparto aun peor. La instruccion localizada no puede
servir como sustituto fiel de una expresion C libre para el planificador.

Las tres sombras preservan los diez cuerpos vecinos, `.rodata` de 32 B,
`.data`/`.bss` vacias y el perfil de ocho reubicaciones del candidato (cuatro a
`$LC0/$LC1` y cuatro ramas internas). La sombra `c0` reproduce 11/11 cuerpos y
todas las secciones no-debug del objeto fuente de produccion.

No se modificaron `pathnode.cpp` ni sus objetos. Queda vedada esta familia de
`fdivs` inline; una proxima palanca tendria que alterar el orden de pseudos sin
introducir un limite de scheduling, o contrastar SN BUILD v1.83.

## Gate

```text
python scratchpad/codex_20260908_iter5_libs/audit_negative.py
python scripts/lcfix.py pathnode --check
```

Artefactos: `scratchpad/codex_20260908_iter5_libs/`.
