ESCRITO: `C:\Users\jferr\Desktop\nfsdecompiled\docs\analisis\r63-ghidra-veredicto.md` (22 KB). Todo medido hoy contra `build/GOWE69/obj/*.o` (objetivo, con reubicaciones) frente a `build/GOWE69/src/*.o` (nuestro). Nada de `src/`, `config/`, `configure.py`, `build/` ni `ghidra_project/` tocado (`git status --porcelain` de esas rutas: 0 lineas).

**EL RESULTADO QUE CAMBIA EL FRENTE.** Las dos fases anteriores compararon el C de Ghidra contra el TEXTO de nuestro fuente. Comparado contra nuestro OBJETO, de los 8 hallazgos de forma **7 son falsos**:

- Conteo de llamadas (`bl` + `bclrl`/`bcctrl`, los dos lados): **28 de 28 IGUAL**, mismo destino y multiplicidad. `VP6` 4 y 4 (nuestro fuente tiene CUATRO `FilterBlock` en `criticalpath.c:1555/1559/1565/1573` y el compilador las funde en una); `RenderFlaresOnCar` 18 y 18 con `eRenderLightFlare` 2 y 2 (nuestro fuente tiene tres).
- Orden de stores: `IdctColumn` emite `dest[56]` PRIMERO en los DOS (`0244 stw r9,224(r15)`) aunque el fuente escribe ascendente; `IdctRow` igual; `Effect_Init` emite rampa antes que seno en los DOS (resuelto por las reubicaciones `HA/LO` de `g_iRampUpTable`/`g_iQuarterSineTable`, identicas); `eProject` diez stores identicos en offset y registro; `CookValues` con **cero** palabras estructurales.
- Sobrevive **uno**: `TrackCarCameraMover::Update`, `look_offset` en orden 152,144,148 contra 144,148,152 — y aun ese puede ser planificacion (las 17 palabras son una permutacion).

**Perfil de las 28** (`r63-veredicto-perfil.py`): 8.417 instrucciones — 6.798 identicas (80,8 %), **1.349 difieren solo en registro (16,0 %)**, 442 estructurales (5,3 %). Seis funciones sin ni una palabra estructural: HolePunch, ActualRead, InitAtSegment, DefragmentPool, HandleTriggers, CookValues.

**Andamios**: censo propio 727 en 186 ficheros (384 barreras, 265 pines, 78 `asm{}` de MWCC). De 18.432 funciones en `report.json` solo 29 estan por debajo del 100 %, asi que **696 de los 727 viven en codigo que ya casa**. En juego hay 31, en 10 funciones. **La interseccion "andamio Y forma que difiere de verdad" es CERO** (con los hallazgos sin verificar habrian sido 4 funciones / 10 andamios). Los tres ensayos que propongo son de RETIRADA de andamio: VP6 (5 pines puestos para una forma que el compilador funde), UpdatePlatInfo (5 andamios y 1 palabra estructural de 511), CookValues (1 andamio, 0 estructurales, 65 instrucciones).

**Reordenacion: NO, con datos.** tau de Kendall contra el orden del objetivo — parseo `+0,953 / +0,998 / +0,989 / +0,938 / +1,000` (zPhysics, zWorld, zCamera, zWorld2, zMisc) contra grafo de llamadas `+0,672 / +0,529 / +0,541 / +0,418 / +0,469`; el parseo gana tambien en las descolocadas. Mecanismo: **460 de las 718 funciones de zPhysics (64,1 %) no tienen ni un `R_PPC_REL24`**; 407 solo estan referenciadas desde vtables, 53 no lo estan de ninguna forma, y de las 235 descolocadas el 94,5 % no tiene llamador. Incluyo el negativo de mi propia primera version, que daba "el grafo gana" por una filtracion (usaba la posicion del objetivo como desempate).

Scripts publicados y verificados desde la raiz del repo: `docs/analisis/r63-veredicto-{elfo,llamadas,perfil,orden,andamios}.py`. Trabajo en `scratchpad/ghidra63/veredicto/` (ademas `stores.py` y `vermdiff.py`).