# Iter3, 2026-09-08: vm cerrada, +168 B exactos

`__VMAllocVirtualToARAMLUT` pasa de 164/168 B al 96,07143 % a **168/168 B,
100 %, 42 instrucciones**. La unidad queda **18/18 exacta, 2.304 B**.
No se modifica configuracion, splits, cabeceras ni otras funciones.

## Nueva palanca, sin repetir las formas C/pragmas/versiones agotadas

El ELF requiere `mr r4,r3; addis r3,r4,1; stw r4,g_baseVMtoARAM; addi
r3,r3,-0x8000`. Las nueve variantes C anteriores fusionaban la base con el
argumento de OSSetArenaLo y eliminaban el mr. Dos pruebas nuevas y acotadas:

| copia | cambio | tamano | similitud |
|---|---|---:|---:|
| baseline | fuente vigente | 164/168 | 96,07143 % |
| producer | productor MW mr/addis/stw con r4 fisico | 168/168 | 95,95238 % |
| virtual_copy | copia definida saved_base, sin hardreg | 168/168 | **100 %** |

La primera cierra la cabecera pero reserva r4 y desplaza los registros del
bucle: 18 filas distintas. No se retiene. La segunda permite que MW asigne y
libere r4 normalmente. La dependencia real impide eliminar el mr: saved_base
debe conservar la base original para el store mientras base ya contiene su
mitad alta incrementada. No hay operandos sin definir ni consumos fantasma.

La suma es exacta: `base + 0x10000 - 0x8000` equivale a
`base + VM_VIRTUAL_LUT_ENTRIES * sizeof(u32)` (0x2000 * 4). El store publica
la base original antes de OSSetArenaLo. El bucle C mantiene las 8.192 escrituras
de cero. Solo se expresan en ASM tres instrucciones reales de las 42 totales.

## Gate y revision independiente

Artefactos en `scratchpad/codex_20260908_iter3_vm`. Compilacion exacta del
objeto real: 1 unidad, 1 OK. `audit_shadow.py production` verifica:

- Baselines fuente/objeto y configuracion/splits/build.ninja con hashes
  inmutables; fuente byte-identica fuera de la funcion y de su nueva nota.
- Solo cambia esta funcion. Las otras 17 conservan sus bytes aunque su
  direccion posterior se desplace 4 B; todas las secciones de datos intactas.
- Identidades, tipos y addends de todas las relocaciones. Se compensa solo
  el incremento probado de 4 B y el primer store que se agenda 8 B despues.
- Auditor ELF: 18 OK; funcion nueva 168 B, 1 rama, 11 relocaciones.

La unica excepcion de nombre del auditor es **preexistente**:
`nextARAMPageToCheck$155` corresponde a `g_nextARAMPageToCheck$233_804FFD9C`.
Un segundo agente demostro el LOCAL OBJECT original de 4 B en .sbss:804FFD9C;
antes/control/despues mantienen el LOCAL OBJECT de 4 B en .sbss+0, los mismos
256 B de VMAlloc y sus 7 accesos R_PPC_EMB_SDA21 con addend0. Las 15 relocs de
VMAlloc son iguales; resolviendo ese unico alias se reproducen 256/256 B del
ELF original. No se generaliza una normalizacion de simbolos arbitrarios.

La revision independiente comprobo ademas `.text` completa de la sombra
2304/2304 B identica al target, sus18 cuerpos y relocaciones, y la semantica
de los tres productores. `.data`103 B, `.sbss`4 B y `.comment` no cambiaron.

```text
python scratchpad/codex_20260908_iter3_vm/audit_shadow.py production
python scripts/fncmp.py LibSN/vm
```

El auditor generico sin el alias demostrado sigue avisando sobre VMAlloc;
usar el gate acotado para comprobar la unidad entera. Esto no constituye
promocion a linked ni autorizacion para modificar su configuracion.
