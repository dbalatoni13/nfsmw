# El commit 3bb4ab24 de upstream: que nos sirve

## Veredicto en una linea
106 hunks revisados, **5 sobreviven** (3 ADOPTAR + 2 PROBAR) y solo **dos mueven bytes reales: 69 B de `.rodata` en zWorld**; los otros 101 se caen porque nuestro arbol va por delante — 55 son "ya lo teniamos", 35 son regresion directa sobre funciones que hoy estan al 100 %, y 11 son cosmetica pura (`this->`, renombrados, `0x400` contra `1024`). La proporcion es la esperada: su ultimo hito es el merge-base `59f184a0` ("5% linked!") y nosotros vamos por 522/616.

## Lo que adoptamos
Ordenado por probabilidad x bytes.

**1. `CarLoader.cpp:2123` — el literal largo del `bMalloc` de `DefragmentPool`** · ADOPTAR · clase *constante* · unidad **zWorld** · agente **world**
Nuestro `bMalloc(128, 0, 0, ...)` pasa a `bMalloc(128, "CarLoaderDefrag but with a really long debug name!!", 0, ...)` (suyo en `3bb4ab24:CarLoader.cpp` mismo bloque). Son **52 B de `.rodata` con contexto identico a los dos lados**, asi que no hay que adivinar posicion: el objetivo (`build/GOWE69/main.dol`) tiene `CarLoaderDefragment\0` + el literal largo + `CARS\TEXTURES.BIN\0`, y nuestro `zWorld.o` tiene los dos vecinos y el hueco en medio. Vecino izquierdo = `CarLoader.cpp:1988`, vecino derecho = `CarLoader.cpp:2289`.
Hay que **quitar a la vez** el `.asciz` de `src/Speed/Indep/SourceLists/zWorld.cpp:257` (si no sale duplicado, +52 B).
**Observables (los dos):** (a) `zWorld.o` debe contener la secuencia exacta `CarLoaderDefragment\0CarLoaderDefrag but with a really long debug name!!\0CARS\TEXTURES.BIN`; (b) `python scripts/fndiff.py zWorld DefragmentPool__9CarLoader` debe SEGUIR en 684 B y no pasar de sus 23 filas (99,269 %) — si sube, revertir. Despues, `scripts/lcfix.py` obligatorio: desplaza la numeracion de los 63 `zWorld.o:$LCnnn` de `config/GOWE69/keep.lst` (`memory/nfsmw-lc-se-desplaza`).
La veda `previo.py DefragmentPool` (bloque L2006, 19 rondas) es TODA sobre reparto de preservados; **ni una sobre literales**.

**2. `CarLoader.cpp:634` — el bucle muerto de sponge en `SetMemoryPoolSize`** · PROBAR · clase *constante* · unidad **zWorld** · agente **world**
Su bucle es real pero **su nombre no**: `CarLoaderSponge` sale 0 veces en `main.dol`; el literal que el objetivo tiene en ese hueco es `SpongeAllocation` (17 B), entre `Cars\0` (de `CarLoader.cpp:631`) y `QUEUED `. Refuerzo: `CarLoaderMemorySpongeAllocSize` y `CarLoaderMemorySpongeNumAllocs` (`CarLoader.cpp:53-54`) no los referencia nadie en todo `src/` — son los argumentos de ese bucle.
Forma a probar: `this->NumSpongeAllocations = CarLoaderMemorySpongeNumAllocs;` + `for (int n = 0; n < this->NumSpongeAllocations; n++) this->SpongeAllocations[n] = bMalloc(CarLoaderMemorySpongeAllocSize, "SpongeAllocation", 0, (CarLoaderMemoryPoolNumber & 0xF) | 0x2000);`, quitando el `.asciz` de `zWorld.cpp:252`.
**Observables:** (a) `SetMemoryPoolSize__9CarLoaderi` tiene que SEGUIR en 304 B y 97,36842 % — si crece, el bucle no se ha borrado y se revierte en el sitio; (b) `zWorld.o` pasa de `Cars\0\0\0\0@ \0\0` a `Cars\0\0\0\0SpongeAllocation`. Nueva entrada en `keep.lst` + `lcfix.py`.
Las 11 variantes de la veda L511 son todas sobre el orden `stw`/`addi` de `CarLoaderMemoryPoolNumber`: **no cubren este hunk**.

**3. `ScratchPtr.h:40` — el cuerpo de `_Alloc`** · ADOPTAR · clase *unsolved-resuelto* · unidad **zPhysicsBehaviors** · agente **phys**
`int i` -> `unsigned int i`, y `spbuffer` queda declarado pero MUERTO (se escribe `mPointer[i]` en las dos ramas), con lo que se cae nuestro `__asm__("")` de `:49`. **Cero bytes**: `fndiff.py zPhysicsBehaviors _Alloc__t10ScratchPtr1ZQ29RigidBody8Volatile` ya da `100.0% size=108/108` por los dos lados (la hipotesis `cmplwi` esta refutada: nuestro `int i` ya emite `cmplwi r10,0x3f` por promocion integral). Lo que se gana es **fidelidad de DWARF**: `dwbody.py` da en el original `unsigned int i; // r10` y `spbuffer` sin registro; en el nuestro `int i` y `spbuffer // r0`, y el bloque lexico interior mide 44 B contra los 12 B del original.
**Es todo o nada**: quitar solo el `__asm__("")` sin la reescritura cambia el reparto (se invierten `stwx`/`add` y se permutan r3/r7/r8).
**Observables:** debe moverse `dwbody.py ... both` (`int i`->`unsigned int i`, `spbuffer` pierde el registro, el bloque encoge hacia 12 B); **no puede moverse ni un byte** en `fndiff` (108/108 en las dos instanciaciones) ni en `fncmp` de la unidad (1 de 1.120).
**AVISO:** copiar su fichero entero seria una regresion de 916.004 B — su `IMPLEMENT_SCRATCHPTR` sigue con el `= {}` que quitamos en `4925e9e2`. **Solo el cuerpo de `_Alloc`.**

**4. `CarLoader.cpp:1191` — borrar el `// UNSOLVED` de `CarLoader::CompositeSkin`** · ADOPTAR · unidad **zWorld** · agente **world** · 0 bytes
`CompositeSkin__9CarLoaderP10LoadedSkin` = 168 B, 100,0 %. Marcador rancio: `memory/nfsmw-promocion-sin-su-cierre` al reves.

**5. `CarRenderConn.cpp:570` — quitar los `#pragma scheduling off/reset` de `UpdateParts`** · PROBAR, prioridad BAJA · unidad **zWorld** · agente **world** · 0 bytes
La funcion ya casa al 100 % CON el pragma; su forma (asignacion incondicional de `mPartState` fuera del if, sin pragma) es una hipotesis limpia de por que el objetivo no necesita la barrera (`memory/nfsmw-andamios-caducan`). **Observable:** seguir al 100,0 % y zWorld en 578/582, con los dos pragmas fuera. Si baja, revertir.

**Limpieza de comentarios rancios** (0 bytes, un commit aparte cuando la tanda acabe, agentes **world** y **phys**): `// UNSOLVED` vivos en funciones que ya estan al 100 % — `NeuQuant.cpp:266,279,334`, `ParameterMaps.cpp:187,232` (+ el `// TODO regswap` de `:224`), `SkyRender.cpp:217`, `VisualTreatment.cpp:18,181,271`, `rain.cpp:122,372`, `CarRender.cpp:4043` (`make_chain`), `CarRenderConn.cpp:569`, `DrawVehicle.cpp:153`.

## Lo que NO, y por que

**Ya lo teniamos (55).** Es el grueso. Ejemplos que valen por todos: `SuspensionTraffic.cpp:752` — su `0.0f`->`0.3f` en el `UMath::Max(d2*4.0f-3.0f, ...)` ya esta escrito en nuestra linea; `VisualTreatment.cpp:444` — su `PLAYER_ALL`->`PLAYER_LOCAL`, el unico cambio de enum con efecto real del commit, ya lo tenemos; `CarLoader.cpp:1198` — su do/while->while ya lo cambiamos nosotros primero (el merge-base tenia el do/while); `NeuQuant.cpp:370` — el bucle de `radpower` con `i` en vez de `j`, que si era reparto de registros, ya lo teniamos; `RigidBody.cpp:842/885` — el orden de argumentos del `Clamp` que ellos "arreglan" ya estaba bien. Y funciones enteras que ellos reescriben para llegar donde estamos: `CarRenderInfo::Render` (11.132 B al 100 %), `DrawAmbientShadow` (2.548 B), `DrawKeithProjShadow` (1.692 B), las tres `CompositeSkin` de `CarSkin.cpp`.

**Seria regresion (35).** Tres familias:
- *Quitar un andamio sin dar la alternativa.* `UMath.h:613/615`: poner `inline` en la declaracion **borra la funcion entera** — medido, `oi_theirs.s` son tres lineas y cero simbolos (mecanismo `DECL_COMDAT`); `OrthoInverse__FRQ25UMath7Matrix4` son 148 B al 100 % en zFoundation y lo referencian cinco SourceLists. `CarRender.cpp:5004`, `VisualTreatment.cpp:505`, `rain.cpp:361/401/473`, `CarRender.cpp:393/477/4130/4915`: su fichero no lleva nuestros bloques `asm()` de datos — **sin ellos zWorld no enlaza** (eran 5 de los 42 indefinidos del proyecto). `CarRender.cpp:2232` (`UpdateLightStateTextures`): quitar la barrera cuesta 28 B medidos. `CarRender.cpp:2334` (`CreateCarLightFlares`): quitan nuestros tres pines y dejan `// TODO regswap` — la confesion de que ellos no la han cerrado.
- *Ya medido y negativo en nuestro arbol.* `CarRender.cpp:2092` (ternario contra if/else, veda r11), `:2686` (el `= 0.0f` de `base_brakelight_intensity`, veda r49: "las cuatro variantes dan objeto IDENTICO"), `:2844` (los `lbl_` a literales `0.0f`: da el tamano exacto pero baja de 98,618 % a 98,054 %, veda r14-r46), `:363` (`CullParts`, veda r50 mide su version exacta como el negativo), `CarLoader.cpp:2177` (su `extern int ChunkMovementOffset;` contra nuestro `enum { kHoleProbeSize = 1 };` — la veda r36b lo cerro a favor de la etiqueta de tipo por el bloque lexico VACIO del DWARF).
- *Apostar contra un 100 %.* Todo el bloque de fisica: `SuspensionSimple::Tire::UpdateLoaded` (1.972 B), `SuspensionSimple::DoWheelForces` (2.860 B), `SuspensionTrailer::DoWheelForces` (2.168 B), `SuspensionRacer::DoWheelForces` (3.156 B) — reagrupaciones de `fmuls`, fusiones de sentencias y permutas de temporales sobre funciones al 100,00000 %. Mencion especial a `SuspensionRacer.cpp:2005`: su `int i`->`unsigned int i` es el unico hunk de la categoria *tipo/firma* que cambiaria codigo de verdad (`cmpw`/`cmplw`) y **el objetivo emite la comparacion CON SIGNO** — nuestro `int` es la firma correcta.
- *Regresiones de datos que ellos aun no han visto.* `ProfileNode profile_node("TODO", 0)` en siete sitios de `CarRender.cpp`/`CarRenderConn.cpp`, en `SkyRender.cpp:304` y como contexto en `CarLoader.cpp:2094`: `TODO\0` sale **cero veces** en todo `main.dol`, y una cadena nueva desplaza todos los `$LC` posteriores. Y `SuspensionRacer.cpp:1105`: su `SteeringInputData` deja `SteeringInputRangeData` a cero referencias — medido en las `.rela.text` del objeto extraido (2 y 2 a cada lado, igual que el nuestro). Son arrays con valores distintos, no un alias.

**Cosmetico / irrelevante (11).** ~100 sitios de `this->` explicito, el renombre `m`->`tm` en `bMath.hpp:1243`, `0x400` contra `1024` en `NeuQuant`, `->` contra `.` en `SuspensionTrailer.cpp:578`, los radianes en crudo contra `bDegToRad(15.0f)` (mismos bits), y `0.035f` contra `RefSkyScale` en `SkyRender.cpp:326` — que era el candidato de constante del grupo y se cae solo: `SkyRender.cpp:36` ya declara `static const float RefSkyScale = 0.035f;`, mismo valor, plegado al mismo `$LC`.
Dos que parecian peligrosos y salieron neutros, medidos con mini-TU y ngccc real: `Ecstasy.hpp:556` (su reescritura de `eFrameMallocMatrix` — `diff` = solo la linea `.file`) y `Ecstasy.hpp:554` (`static inline`->`inline`: ni `.weak` ni `.gnu.linkonce`, ni usada ni sin usar). Neutro en una cabecera que tocan 74 TU y cuatro unidades con agente vivo = negativo.

## Lo que este commit nos dice de upstream
**No van por delante en nada de codigo.** Los 14 `// UNSOLVED` que quitan estan **todos** sobre funciones que nosotros ya tenemos al 100 %, y en las dos que miramos con lupa su solucion es distinta de la nuestra y la nuestra es la que casa: `RigidBody::Mesh` (ellos repiten la expresion, nosotros extraemos `nbytes`, 160 B al 100 %) y `GetValueFromSpline` (ellos reasocian, nosotros sacamos temporales en `VisualTreatment.cpp:24-25`, 96 B al 100 %). El unico UNSOLVED del que sacamos algo — `ScratchPtr::_Alloc` — no nos da bytes, nos da DWARF.
Y en varios sitios van *hacia atras*: su `IMPLEMENT_SCRATCHPTR` con el `= {}`, su `SteeringInputData`, sus literales `"TODO"`.
**Lo unico realmente valioso son los dos literales de `bMalloc` de `CarLoader.cpp`** — que es exactamente el apartado (a) del encargo: datos que acaban en `.rodata` y mueven el DOL, el hallazgo mas caro de encontrar por nuestra cuenta y el mas barato de copiar.
**Recomendacion:** seguir estos commits, pero **filtrando solo por literales de cadena y por constantes numericas**. Todo lo demas es ruido a este ritmo de divergencia: 4,7 % de aprovechamiento y dos hunks que, si alguien los adopta sin leer las vedas, tiran el enlace de zWorld.

## Riesgo de cabecera compartida
Solo un adoptado toca cabecera: **`ScratchPtr.h`**, incluida por 9 ficheros (`RigidBody.h:14`, `SimpleRigidBody.h:12`, `SimpleRigidBody.cpp:7` y transitivos). Pero `report.json` **solo encuentra simbolos `ScratchPtr` en UNA unidad**: `zPhysicsBehaviors`, con 11 — nadie mas instancia las plantillas. **Riesgo de arrastre a unidades sin agente: NULO**, y la unidad afectada la lleva **phys**.
Los otros cuatro adoptados son `.cpp` de zWorld (agente **world**) y `CarRenderConn.cpp`, tambien zWorld. Ninguna unidad sin agente queda expuesta.
Las cabeceras que SI alcanzan a unidades sin agente — `UMath.h` (93 includes, 6 SourceLists), `Ecstasy.hpp` (74 includes, 20 sitios de llamada), `bMath.hpp` (todo el arbol) — estan **todas rechazadas**.

---
El segundo commit, **`5653517c`**, se coge tal cual: anade `pyelftools` a `requirements.txt`, y es una dependencia real sin declarar que usan `tools/dwarf1_gcc_line_info.py`, `tools/dwarf1_gcc_split_info.py` y `tools/flag_permuter.py`.